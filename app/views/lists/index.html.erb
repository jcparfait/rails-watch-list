<div class="home-page">
  <section class="hero-section">
    <div>
      <p class="eyebrow">Watch List</p>
      <h1>My movie lists</h1>
      <p class="hero-text">
        Create curated collections and save the films you want to remember.
      </p>
    </div>
  </section>

  <section class="lists-grid">
    <% @lists.each do |list| %>
      <% fallback_image_url =
        case list.name.downcase
        when /drama/
          "https://images.unsplash.com/photo-1485846234645-a62644f84728?q=80&w=1200&auto=format&fit=crop"
        when /comedy/
          "https://images.unsplash.com/photo-1527224538127-2104bb71c51b?q=80&w=1200&auto=format&fit=crop"
        when /classic/
          "https://images.unsplash.com/photo-1505686994434-e3cc5abf1330?q=80&w=1200&auto=format&fit=crop"
        when /rewatch/
          "https://images.unsplash.com/photo-1517604931442-7e0c8ed2963c?q=80&w=1200&auto=format&fit=crop"
        else
          "https://images.unsplash.com/photo-1489599849927-2ee91cede3ba?q=80&w=1200&auto=format&fit=crop"
        end
      %>

      <% image_url =
        if list.photo.attached?
          cl_image_path list.photo.key, height: 400, width: 600, crop: :fill
        else
          fallback_image_url
        end
      %>

      <%= link_to list_path(list),
                  class: "list-card",
                  style: "background-image: linear-gradient(180deg, rgba(10, 10, 15, 0.12), rgba(10, 10, 15, 0.88)), url('#{image_url}');" do %>

        <div class="list-card-content">
          <span class="list-card-label">Collection</span>
          <h2><%= list.name %></h2>
          <p><%= pluralize(list.movies.count, "movie") %></p>
        </div>

      <% end %>
    <% end %>
  </section>
</div>
